# Agent Instructions

## Active remediation authority

```text
active_program=BWS_FINAL_REMEDIATION_R01_R12_V1
activation_state=ACTIVE
current_admitted_tranche=BWS-W4-T39
current_campaign_order=1
current_stage=S1
current_task_source=docs/remediation/BWS_FINAL_REMEDIATION_R01_R12_V1/activation/tasks/001-BWS-W4-T39.md
pre_s2_existing_controllers=prohibited
post_s2_controller=run-autonomous-implementation.sh
canonical_node=v20.20.2
```

Operate exactly one source-mutating tranche at a time. Reverify every mapped finding against current source before editing. Stop on moved, resolved, contradicted, or out-of-scope evidence. Emit exact preimage, postimage, test, environment, and result receipts. Do not infer acceptance from documentation, static markers, listed-but-unrun tests, or Node 22 output.

The active launcher, admission, tasks, and state model are under `docs/remediation/BWS_FINAL_REMEDIATION_R01_R12_V1/activation/`. Before all S2 tranches are accepted, do not start `run-autonomous-implementation.sh`, `run-autonomous-bugfix.sh`, `run-paper-evaluation.sh`, `run-paper-autopilot.sh`, or `run-bugfix-autopilot.sh` as remediation drivers. After S2, only the active launcher may invoke the repaired implementation controller for one exact admitted tranche.

## Repository purpose

```text
repo_role=surebet_strategy_application
upstream_platform=betting-win
provider_truth_owner=betting-win
canonical_history_owner=betting-win
strategy_state_owner=betting-win-surebet
backtesting_owner=betting-win-surebet
paper_mode_owner=betting-win-surebet
future_live_decision_owner=betting-win-surebet_after_explicit_gate
account_policy=separate_from_betting-win-betting
```

The repository owns surebet logic, `surebet.*` persistence, backtesting, private paper evaluation, BWS runtime evidence, and future separately authorized execution decisions. It does not own provider adapters or canonical provider history.

## Source-of-truth order

1. Current source and tests.
2. Fresh retained evidence generated by current source.
3. Active admission, plan, tasks, and receipts under `docs/remediation/BWS_FINAL_REMEDIATION_R01_R12_V1/activation/`.
4. The first active-remediation sections of `docs/repo_status_current.md` and `docs/automation/current-implementation-task.md`.
5. `docs/000_documentation_index.md` and `docs/documentation-inventory.json`.
6. Supporting current contracts.
7. Historical review, blueprint, and compatibility records.

The later BWS-600 sections embedded in the two immutable current-authority documents are retained only because current validators still assert their text. They are not the active route.

## Remediation execution discipline

- Follow `unattended-plan.json` in exact campaign order.
- Orders 1 through 13 use direct bounded Codex sessions through the active launcher.
- Orders 14 through 47 may use repaired `run-autonomous-implementation.sh` only after the S2 gate passes.
- Preserve one primary owner and serialize shared paths.
- Change only the admitted correction boundary and exact protected-file allowlist.
- Missing environment capability, unknown state, receipt mismatch, dependency failure, or stale source is a blocker.
- Keep BWS-600, BWS-710, BWS-900, release, deployment, and live execution unchanged.
- Never access or mutate a `betting-win` checkout. `BETTING_WIN_REPO_PATH` remains historical BWS-600 context and is not authorized for this campaign.

## Implementation discipline

Preserve repository structure, names, types, helpers, error handling, fail-closed validation, and deterministic outputs. Avoid unrelated refactors and hidden fallbacks. Missing or invalid configuration must fail explicitly. Preserve validated solver, completion, exposure, settlement, report, API and cockpit behavior unless the admitted tranche expressly owns it.

## Protected automation policy

Protected files are listed in `docs/automation/PROTECTED_AUTOMATION_FILES.md`. Ordinary cycles may not edit them. A remediation task may edit only the exact paths in its `allowed_protected_files` field and only while the activation validator confirms the matching tranche.

## Process and runtime safety

Do not start, stop, restart, detach, replace, or broadly kill pre-existing services or processes. Test-owned loopback children must have exact ownership, bounded lifetime, and cleanup. Do not deploy, migrate a non-disposable database, access providers, sign, broadcast, pay, place orders, or promote production.

## Validation

Use exact Node `v20.20.2` for acceptance. Run the focused, production-entrypoint, negative, adversarial, concurrency, crash/restart, and environment proof required by the admitted tranche. A test named in a packet but not executed closes nothing.

## Standard automation surface

```text
update_git.sh
zip_codebase.sh
pull_artifacts_and_zip_codebase.sh
run-autonomous-implementation.sh
run-paper-evaluation.sh
run-autonomous-bugfix.sh
automation.config.sh
docs/automation/
.automation/
```

`update_git.sh --acp` preserves `GITHUB_TOKEN` support. `zip_codebase.sh` creates numbered repository-root ZIPs and no manifest. `pull_artifacts_and_zip_codebase.sh` pulls server `artifacts.zip`, then calls local `zip_codebase.sh`, and does not use `automation.config.sh`. `stop-autonomous-run.sh` is intentionally absent.

## Retained completed ledgers

`backlog/bws_full_implementation.csv` and `backlog/bws_remaining_safe_local_map.csv` are completed platform traceability, not active routing.

## Historical validation lineage

`BWS_FULL_PLATFORM_IMPLEMENTATION_V1` and validated carry-forward markers `BWS-592`, `BWS-593`, and `BWS-599` remain regression authority.

## Retained pre-remediation compatibility record

The exact markers and sentences below are non-routing history required by current repository validators.

```text
program=BWS_B1_CROSS_VENUE_OFFLINE_FALSIFICATION_V1
current_task=BWS-600
selected_controller=run-paper-autopilot.sh
active_implementation_queue=none
bws600_current_task=BWS-600
safe_local_terminal_gate=BWS-599
BWS-900=parked
```

The reviewed B1 cross-venue offline falsification program is validated through BWS-820 for dependency-ready local rows. The BWS-700 implementation queue `backlog/bws_b1_cross_venue_implementation.csv` is complete for dependency-ready local rows through `BWS-820`. Use `run-paper-autopilot.sh` for the current `BWS-600` runtime-evidence gate; use `run-autonomous-implementation.sh` only for a future reviewed source handoff or unblocked BWS-710 intake. These sentences describe the superseded route immediately before remediation activation.

## graphify

The repository-retained Graphify skill is under `.codex/skills/graphify/`. It is optional documentation tooling and not remediation authority.
