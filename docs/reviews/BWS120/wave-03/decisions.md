# Cumulative consolidation decisions through Wave 03

## BWS117-W1-DEC-001

Use betting-win-surebet117.zip as the exact documentation preimage while classifying all three BWS116 reviews as COMPATIBLE_AND_UNAFFECTED_BY_DELTA because every member path, byte, size, and mode is identical.

Owner: `Wave 01 consolidation`. Blocks release: `false`.

## BWS117-W1-DEC-002

Retain all 41 confirmed finding IDs unchanged. No confirmed findings are merged because no pair shares the same trigger, root cause, and minimal correction boundary.

Owner: `Wave 01 consolidation`. Blocks release: `false`.

## BWS117-W1-DEC-003

Consolidate BWS116-R01-HO-002 and both KNOWN_BASELINE_MANIFEST_DRIFT records under canonical non-defect handoff ID KNOWN_BASELINE_MANIFEST_DRIFT with R11 ownership.

Owner: `R11`. Blocks release: `true`.

## BWS117-W1-DEC-004

R01 owns immutable upstream cycle identity, provenance, and exact consumed-record semantics. R03 owns the atomic persistence and checkpoint mechanics required to store that cycle.

Owner: `R01/R03`. Blocks release: `true`.

## BWS117-W1-DEC-005

R02 owns complete-set, provider/generation, quote, fee, capacity, and solver semantics after intake. R01 remains owner of upstream authority and currentness before records cross into the strategy layer.

Owner: `R01/R02`. Blocks release: `true`.

## BWS117-W1-DEC-006

R01 pagination/HTTP deadlines, R03 psql command deadlines, and R03 service cancellation are related dependencies but distinct root causes and must not be merged.

Owner: `R01/R03`. Blocks release: `true`.

## BWS117-W1-DEC-007

Retain BWS116-R03-001 as P0. Current supplied migrations are presently surebet-qualified; the defect is the callable migration source/scope authority that accepts external and cross-schema executable SQL.

Owner: `R03`. Blocks release: `true`.

## BWS117-W1-DEC-008

Current task/status and BWS-600/BWS-710/BWS-900 routing documents remain untouched. Review documentation is subordinate and cannot promote readiness.

Owner: `Current task authority`. Blocks release: `false`.

## BWS117-W1-DEC-009

Node 22 observations remain supplementary. Any implementation acceptance must rerun the relevant suite under exact Node 20.20.2.

Owner: `R11 and implementation validation`. Blocks release: `true`.

## BWS117-W1-DEC-010

R03 PostgreSQL findings supported only statically remain confirmed source defects where the code path is direct, but PostgreSQL isolation, crash, and role enforcement acceptance remains unproven until a disposable database run.

Owner: `R03/R08/R11`. Blocks release: `true`.

## BWS117-W1-DEC-011

Do not integrate or modify betting-win. Prior BW R13/R17/R18 material and its failed documentation overlay remain out of scope and process-only evidence.

Owner: `Repository boundary`. Blocks release: `false`.

## BWS117-W1-DEC-012

The exact archive and validate_source_manifest.py result override the stale PROJECT_STATUS prose claiming the previous manifest diagnostic was resolved. The review overlay records the contradiction but does not rewrite protected status; R11 owns correction.

Owner: `R11`. Blocks release: `true`.

## BWS117-W1-DEC-013

Wave 02 R04-R06 may proceed in parallel as read-only research. No source implementation starts before all review outputs are consolidated and dependency-ready fix tranches are approved.

Owner: `Review program`. Blocks release: `false`.

## BWS118-W2-DEC-001

Use betting-win-surebet118.zip as the exact Wave 02 documentation preimage. Its 657 paths and bytes match all three review coverage manifests.

Owner: `Wave 02 consolidation`. Blocks release: `false`.

## BWS118-W2-DEC-002

Classify the Wave 01 R01-R03 overlay as LANDED_IN_CURRENT_REPOSITORY because every one of its 40 postimages is present in BWS118 and exactly equals the supplied overlay package.

Owner: `Wave 02 consolidation`. Blocks release: `false`.

## BWS118-W2-DEC-003

Retain all 44 new R04-R06 finding IDs. No new finding shares both root cause and minimal correction boundary with an inherited or concurrent finding.

Owner: `Wave 02 consolidation`. Blocks release: `true`.

## BWS118-W2-DEC-004

R04 owns simulation event identity and lifecycle representation; R03 owns durable atomic create-or-compare and crash recovery. Related manifestations are dependencies, not duplicates.

Owner: `R04/R03`. Blocks release: `true`.

## BWS118-W2-DEC-005

R02 owns planned quote/fee/solver economics; R04 owns actual-fill terms, residual exposure, settlement, correction, finality, and falsification semantics.

Owner: `R02/R04`. Blocks release: `true`.

## BWS118-W2-DEC-006

R04 owns lifecycle truth before projection. R05 owns preservation of that truth in API contracts and cockpit presentation.

Owner: `R04/R05`. Blocks release: `true`.

## BWS118-W2-DEC-007

R01 owns upstream pagination/receipt identity. R05 owns BWS-local API cursor, query-scope, snapshot, and continuation semantics.

Owner: `R01/R05`. Blocks release: `true`.

## BWS118-W2-DEC-008

R03 owns repository failures and durable transitions. R05 owns public error taxonomy, sanitization, and empty-versus-failed projection.

Owner: `R03/R05`. Blocks release: `true`.

## BWS118-W2-DEC-009

R05 owns browser request-generation and stale-state behavior. R06 owns process, listener, and service lifecycle cancellation.

Owner: `R05/R06`. Blocks release: `true`.

## BWS118-W2-DEC-010

R03 job leases and R06 process ownership are independent authority layers and require separate fencing primitives.

Owner: `R03/R06`. Blocks release: `true`.

## BWS118-W2-DEC-011

R06 owns health/readiness source truth and CLI/systemd exit semantics; R05 owns faithful display of those typed states.

Owner: `R05/R06`. Blocks release: `true`.

## BWS118-W2-DEC-012

R06 aggregate lifecycle deadlines do not replace R01 HTTP-cycle deadlines or R03 psql/job cancellation. Each remains separately owned.

Owner: `R01/R03/R06`. Blocks release: `true`.

## BWS118-W2-DEC-013

R06 lifecycle evidence filename uniqueness is a source defect. R07 and R12 may consume it as a publication/controller dependency but must not duplicate it.

Owner: `R06/R07/R12`. Blocks release: `true`.

## BWS118-W2-DEC-014

Node 22 observations remain supplementary. Canonical implementation acceptance requires Node 20.20.2.

Owner: `Cross-area runtime`. Blocks release: `true`.

## BWS118-W2-DEC-015

Preserve KNOWN_BASELINE_MANIFEST_DRIFT under R11. Do not regenerate SOURCE_MANIFEST.json in a documentation-only overlay.

Owner: `R11`. Blocks release: `true`.

## BWS118-W2-DEC-016

Current BWS-600, BWS-710, BWS-900, controller, deployment, and runtime authority documents remain byte-identical. Review evidence cannot promote them.

Owner: `Current authority`. Blocks release: `true`.

## BWS118-W2-DEC-017

Wave 03 R07-R09 may proceed in parallel as read-only research. No application implementation is authorized before the complete review program is consolidated.

Owner: `Wave 03 routing`. Blocks release: `true`.

## BWS120-W3-DEC-001

Use betting-win-surebet120.zip and SHA-256 d4565c05ecfe63f5d5511f224a20ff20fb82bfc75b23203c94d7d3e98a8c21bb as the exact Wave 03 documentation preimage.

Owner: `Wave 03 baseline`. Blocks release: `true`.

## BWS120-W3-DEC-002

Classify the corrected Wave 02 overlay package as LANDED_IN_CURRENT_REPOSITORY because all 40 exact postimages and modes are present in BWS120.

Owner: `Overlay lineage`. Blocks release: `true`.

## BWS120-W3-DEC-003

Classify the original Wave 02 installer attempt as FAILED_DURING_VALIDATION_AND_ROLLED_BACK; the Markdown inventory defect was installer-local and rollback restored the preimage.

Owner: `Overlay lineage`. Blocks release: `true`.

## BWS120-W3-DEC-004

Retain all 52 new R07-R09 confirmed IDs. Similar symptoms are dependencies, not identical root causes or correction boundaries.

Owner: `Consolidation`. Blocks release: `true`.

## BWS120-W3-DEC-005

R07 owns evidence identity/publication/currentness; R06 retains lifecycle event generation and process-state ownership.

Owner: `R07/R06`. Blocks release: `true`.

## BWS120-W3-DEC-006

R07 evidence retention and R08 database retention are separate lifecycle authorities and must be repaired independently before coordinated acceptance.

Owner: `R07/R08`. Blocks release: `true`.

## BWS120-W3-DEC-007

R08-002 owns exact database target binding. R09-006 owns the broader upgrade apply-time revalidation of environment, release, and install evidence. Neither is merged.

Owner: `R08/R09`. Blocks release: `true`.

## BWS120-W3-DEC-008

R08 backup replacement, R07 handoff publication, and R09 release publication are distinct transactional publication boundaries.

Owner: `R07/R08/R09`. Blocks release: `true`.

## BWS120-W3-DEC-009

R09 upgrade ownership is distinct from R06 process ownership and R03 migration ownership; the eventual integration requires all three fencing layers.

Owner: `R03/R06/R09`. Blocks release: `true`.

## BWS120-W3-DEC-010

R09 rollback and readiness composition must consume authoritative R06 lifecycle outcomes rather than redefining lifecycle mechanics.

Owner: `R06/R09`. Blocks release: `true`.

## BWS120-W3-DEC-011

R07-017 owns runtime-evidence relationship truth; R09-020 and R09-021 own release/recovery/final-archive generation binding. Preserve all three.

Owner: `R07/R09`. Blocks release: `true`.

## BWS120-W3-DEC-012

Retain all four new P0 findings at P0: log repository escape, secret exposure, unrelated backup deletion, and wrong-database migration authority.

Owner: `Severity`. Blocks release: `true`.

## BWS120-W3-DEC-013

Node 22 observations remain supplementary. Canonical acceptance requires Node 20.20.2.

Owner: `Runtime`. Blocks release: `true`.

## BWS120-W3-DEC-014

PostgreSQL, systemd, multi-process, and multi-hour soak behavior not executed remains explicit environment-dependent evidence, not a pass.

Owner: `Runtime evidence`. Blocks release: `true`.

## BWS120-W3-DEC-015

Preserve KNOWN_BASELINE_MANIFEST_DRIFT under R11. Do not regenerate SOURCE_MANIFEST.json in this documentation-only overlay.

Owner: `R11`. Blocks release: `true`.

## BWS120-W3-DEC-016

Preserve current BWS-600, BWS-710, BWS-900, controller, release, and deployment authority byte-for-byte.

Owner: `Current authority`. Blocks release: `true`.

## BWS120-W3-DEC-017

Exclude offensive or destructive reproduction. Defensive wrong-path, wrong-database, secret-redaction, and confinement findings remain in scope.

Owner: `Safety`. Blocks release: `true`.

## BWS120-W3-DEC-018

Wave 04 R10-R12 may proceed in parallel as read-only research. Application implementation remains unauthorized until final review consolidation or separate explicit authorization.

Owner: `Wave 04 routing`. Blocks release: `true`.

## BWS120-W3-DEC-019

No betting-win checkout, source, documentation, service, or database is part of this BWS overlay or review program.

Owner: `Repository boundary`. Blocks release: `true`.
