# Non-evidence receipt examples

Every example validates against its corresponding schema and is explicitly marked `EXAMPLE_ONLY_NOT_EVIDENCE`. None records an accepted implementation or runtime result.
