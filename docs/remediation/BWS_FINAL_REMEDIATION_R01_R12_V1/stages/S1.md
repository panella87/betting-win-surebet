
# S1: P0 safety corridor

## Current state

`BWS-W4-T39` is the only admitted tranche; no S1 tranche is accepted yet.

## Stage objective

Close the P0 safety corridor and its required configuration/database foundations through bounded operator-driven cycles.

## Ordered tranche list

- BWS-W4-T39
- BWS-W4-T37
- BWS-W4-T38
- BWS-W3-T26
- BWS-W1-T01
- BWS-W1-T09
- BWS-W3-T30
- BWS-W3-T31

## Entry gate

S0 exit receipt accepted; exact predecessor postimage and hold register bound; exactly the first S1 tranche admitted.

## Exit gate

All 10 P0 findings closed with Node 20.20.2 and required filesystem/PostgreSQL proof.

## Why order matters

- BWS-W4-T39 order 1: Must precede normal commit/push/archive workflows because it closes nested-secret leakage, destructive Git-config mutation, and first-use SSH trust.
- BWS-W4-T37 order 2: Defines one bounded effective configuration and database tuple before CLI, migration, restore, process, and release identities depend on it.
- BWS-W4-T38 order 3: Closes P0 caller-CWD execution and plaintext database-password argv, then confines status and log destinations.
- BWS-W3-T26 order 4: Closes P0 log repository escape and secret leakage before any implementation-generated evidence is considered trustworthy.
- BWS-W1-T01 order 5: Closes P0 migration source/schema escape and establishes the restricted database role used by every later database tranche.
- BWS-W1-T09 order 6: Required non-P0 foundation for the P0 database-target tranche: read-only status, exact lineage compatibility, and one migration owner.
- BWS-W3-T30 order 7: Closes destructive backup overwrite before restore/upgrade workflows can rely on a backup generation.
- BWS-W3-T31 order 8: Closes wrong-database migration authority and binds restore proof to the exact target and dump.

## Primary owners

- R03
- R07
- R08
- R10

## Shared paths and serialization points

- `cli.js`
- `docs/037_database_backup_retention_and_recovery.md`
- `open_log.sh`
- `package.json`
- `packages/bootstrap/src/cli/bws-database-lifecycle.ts`
- `packages/bootstrap/src/cli/bws-final-local-acceptance.ts`
- `packages/bootstrap/src/cli/bws-soak-campaign.ts`
- `packages/bootstrap/src/operations/database-lifecycle.ts`
- `packages/bootstrap/src/operations/external-runtime-preflight.ts`
- `packages/bootstrap/src/operations/final-local-acceptance.ts`
- `packages/bootstrap/src/operations/observability.ts`
- `packages/bootstrap/src/operations/operator-lifecycle.ts`
- `packages/bootstrap/src/operations/release-upgrade.ts`
- `packages/persistence/src/migrations.ts`
- `packages/persistence/src/psql.ts`
- `scripts/bws-root-wrapper-runtime.mjs`
- `tests/bws-observability.test.ts`

Every shared path requires the prior owning tranche postimage, one active edit owner, read-only secondary reviewers, and a postimage handoff before the next owner is admitted.

## Required proof environments

- AS_SPECIFIED_BY_REVIEW=55 mapped requirements
- DISPOSABLE_POSTGRESQL_AND_NODE20=15 mapped requirements
- NODE20_OR_LANGUAGE_NEUTRAL_OFFLINE_OR_CONTROLLED_INTEGRATION=43 mapped requirements

## Expected artifacts and receipts

- stage admission or S0 proposal receipt;
- one preimage, current-source reverification, postimage, focused-test, production-entrypoint, and environment receipt per admitted tranche as applicable;
- rollback receipt for any reverted or interrupted cycle;
- one stage-result receipt binding all terminal tranche receipts;
- retained-hold register and next-stage admission decision.

## Rollback and interruption behavior

Stop new writes, classify the active tranche as `BLOCKED` unless an exact rollback completes, restore every changed byte/mode from the admitted preimage, remove only transaction-owned additions, preserve unrelated dirty content, and record ambiguity rather than inferring success. No later tranche may start from an unreceipted or mixed postimage.

## External blockers that may remain

- No external blocker may be substituted for internal proof; failures remain BLOCKED.

## Prohibited promotion behavior

- no documentation, source completion, static marker, unexecuted test, Node 22 run, fixture, mock, local export, declaration, or caller assertion promotes a hold;
- `SOURCE_COMPLETE_EXTERNAL_PENDING` does not admit the next release/deployment/live-execution state;
- S1 and S2 do not use existing autonomous controllers;
- no access to or mutation of `betting-win` is authorized.

## Holds after stage completion

```text
BWS-600=BLOCKED
BWS-710=BLOCKED
BWS-900=PARKED_NOT_AUTHORIZED
release=BLOCKED
deployment=BLOCKED
live_execution=PROHIBITED
```

A separate explicit, evidence-bound decision is required to change any hold.

## Exact next-stage admission rule

After this stage's exit receipt validates, admit exactly the first not-yet-terminal tranche of S2; bind the accepted predecessor postimage, dependencies, exact allowed paths, proof environments, and retained holds. If no such receipt exists, remain `BLOCKED`.
