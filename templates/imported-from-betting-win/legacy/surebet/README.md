# Imported legacy surebet templates

This directory contains historical surebet review templates copied from the original `betting-win` repository.

```text
legacy_surebet_import_status=imported_and_rehomed
active_authority=no
active_template_authority=no
```

Templates here are retained for lineage. Current operator and strategy authority remains in the active docs under `docs/`.
